name: Lint Security

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

concurrency:
  group: ${{ forgejo.ref }}-${{ forgejo.workflow }}
  cancel-in-progress: true

jobs:
  security:
    runs-on: ubuntu-latest-arm64
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          fetch-depth: 0
      # trufflehog@main mounts the checkout at /tmp. The ARM64 runner's action
      # container has a read-only /tmp, so give the scanner a separate writable
      # scratch mount while keeping the checkout read-only.
      - name: Scan Git history for secrets
        run: |
          mkdir -p "$RUNNER_TEMP/trufflehog"
          docker run --rm \
            --volume "$GITHUB_WORKSPACE:/repo:ro" \
            --volume "$RUNNER_TEMP/trufflehog:/tmp" \
            ghcr.io/trufflesecurity/trufflehog:latest \
            git file:///repo \
            --fail \
            --no-update \
            --results=verified,unknown
